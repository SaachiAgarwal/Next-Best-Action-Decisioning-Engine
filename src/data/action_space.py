"""Action space definition.

Defines the set of actions (candidate recommendations) the engine can
take, aggregated at ``ACTION_GRANULARITY`` (see ``src/config.py``).

Placeholder module — implementation lands in a later milestone.
"""
