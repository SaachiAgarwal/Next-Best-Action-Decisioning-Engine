"""Train/validation/test splitting.

Produces time-based splits around ``CUTOFF_DATE`` (see ``src/config.py``)
to avoid leakage in the recommendation setting.

Placeholder module — implementation lands in a later milestone.
"""
