"""Data cleaning layer.

Handles type coercion, deduplication, and null handling on the raw
H&M data before it is used downstream.

Placeholder module — implementation lands in a later milestone.
"""
